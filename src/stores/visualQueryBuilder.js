import { ref } from 'vue'

// Whether the Visual Query Builder panel is showing. One panel for the whole window,
// toggled from the query bar, the View menu, and a field drag onto it.
export const vqbOpen = ref(false)
